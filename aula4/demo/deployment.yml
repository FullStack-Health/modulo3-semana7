apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-health
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello-health
  template:
    metadata:
      labels:
        app: hello-health
    spec:
      containers:
        - name: app
          image: andresnu/hello-health:latest
          ports:
            - containerPort: 8081